import { PricediscountPipe } from './pricediscount-pipe';

describe('PricediscountPipe', () => {
  it('create an instance', () => {
    const pipe = new PricediscountPipe();
    expect(pipe).toBeTruthy();
  });
});
